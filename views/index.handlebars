<ul>
  {{#each burgers}}
   {{#unless devoured}}
  <li>{{this.burger_name}}</li>
   {{/unless}}
  {{/each}}
</ul>

<h2>Add a Burger</h2>
<form class="create-burger">

  {{!-- <div class="form-burger"> --}}
    <label for="burger">Burger:</label>
    <input type="text" id="burger-input" name="burger_name">
  {{!-- </div> --}}
{{!-- 
  <div class="form-group">
    <label for="sleepy">Sleepy?</label><br>
    <input type="radio" name="sleepy" value="1" checked> Sleepy!<br>
    <input type="radio" name="sleepy" value="0"> Awake
  </div> --}}

  {{!-- <div>
    <li>
	{{name}}

	<button class="change-sleep" data-id="{{id}}" data-newsleep="{{sleep}}">
		{{#if sleep}}SLEEP TIME!{{else}}WAKE UP!{{/if}}
	</button>

	<button class="delete-cat" data-id="{{this.id}}">DELETE!</button>
</li>
</div> --}}

  <button class="btn btn-sm btn-primary" type="submit">Add Burger</button>
</form>

